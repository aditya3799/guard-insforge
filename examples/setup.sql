CREATE TABLE IF NOT EXISTS demo_accounts ( id SERIAL PRIMARY KEY, email TEXT UNIQUE NOT NULL, role TEXT DEFAULT 'member' ); CREATE TABLE IF NOT EXISTS demo_tasks ( id SERIAL PRIMARY KEY, title TEXT NOT NULL, status TEXT DEFAULT 'pending' );
